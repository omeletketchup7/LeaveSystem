import { DateToShowPipe } from './date-to-show.pipe';

describe('DateToShowPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToShowPipe();
    expect(pipe).toBeTruthy();
  });
});
